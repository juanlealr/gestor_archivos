<Window x:Class="FileManager.UI.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Entrada de Datos" Height="180" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="Height">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleTextBlock" Grid.Row="0" FontSize="14" Margin="0,0,0,15" Foreground="#212121" FontWeight="Normal"/>

        <TextBox x:Name="InputTextBox" Grid.Row="1" Height="40"
                 Padding="10,8" FontSize="13" Margin="0,0,0,15"
                 Background="White" Foreground="#212121"
                 BorderBrush="#E0E0E0" BorderThickness="1"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Aceptar" Width="90" Margin="0,0,10,0"
                    Click="OkButton_Click"
                    Background="#2196F3" Foreground="White"
                    Padding="10,8" FontSize="12" Cursor="Hand"/>
            <Button Content="Cancelar" Width="90"
                    Click="CancelButton_Click"
                    Background="White" Foreground="#212121"
                    BorderBrush="#E0E0E0" BorderThickness="1"
                    Padding="10,8" FontSize="12" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
