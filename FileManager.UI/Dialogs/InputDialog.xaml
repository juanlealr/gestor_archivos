<Window x:Class="FileManager.UI.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Entrada de Datos" Height="150" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleTextBlock" Grid.Row="0" FontSize="14" Margin="0,0,0,10" Foreground="{StaticResource TextBrush}"/>

        <TextBox x:Name="InputTextBox" Grid.Row="1" VerticalAlignment="Top" Height="35"
                 Padding="10,8" FontSize="13"
                 Background="White" Foreground="{StaticResource TextBrush}"
                 BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Aceptar" Width="80" Margin="0,0,10,0"
                    Click="OkButton_Click"
                    Background="{StaticResource PrimaryBrush}" Foreground="White"
                    Padding="10,8" FontSize="12"/>
            <Button Content="Cancelar" Width="80"
                    Click="CancelButton_Click"
                    Background="Transparent" Foreground="{StaticResource TextBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                    Padding="10,8" FontSize="12"/>
        </StackPanel>
    </Grid>
</Window>
